# [담당: 허겸] 운영 환경 설정 (배포 시 활성화, 값은 모두 환경변수로 주입)
# spring:
#   datasource:
#     url: jdbc:mysql://${DB_HOST}:3306/customhouse?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
#     username: ${DB_USERNAME}
#     password: ${DB_PASSWORD}
#   jpa:
#     hibernate:
#       ddl-auto: validate
#     show-sql: false

ai-engine:
  base-url: ${AI_ENGINE_BASE_URL:http://localhost:8000}

logging:
  level:
    com.customhouse: INFO
